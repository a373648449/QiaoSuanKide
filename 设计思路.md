# 巧算 Kid｜整体设计思路

给大约 6 岁孩子用的 **20 以内加减** 练习。先把凑十、破十、十格图听懂，再求做对、做熟。不是计时口算大赛。

陪伴角色是 **抱抱小熊 / 小熊老师**：孩子还是自己在学，小熊负责加油、讲思路、看完今天的练习后安排明天怎么练。

---

## 1. 教学原则

1. **先思路，后速度。** 听思路、看十格图，比连续刷题更优先。
2. **对错由程序算，不由 AI 算。** `8+6` 永远等于 14。模型不能改答案、不能出超纲题。
3. **能力按年级顺序排。** 10 以内 → 20 不进位/不退位 → 凑十 → 破十 → 混合。
4. **同一关少重复、少连号。** 不要总出 `9+1` 接着 `9+2`。针对性练习则按「热身 → 薄弱题 → 同类巩固」排，和闯关的「打散」不一样。
5. **给孩子的话要短。** 批评不要说，鼓励一句就够。细节给家长看。

---

## 2. 关卡（能力编排）

关卡本身就是技能树，不必再单独做一套 SKILLS 系统。

| 关 | 名称 | 题量 | 过关正确率 | 题型 |
|---|---|---|---|---|
| 1 | 10以内加法 | 10 | 80% | 和不超过 10 |
| 2 | 10以内减法 | 10 | 80% | 被减数不超过 10 |
| 3 | 10以内加减混合 | 12 | 80% | 第 1、2 关混合 |
| 4 | 20以内不进位加法 | 12 | 80% | 个位相加不满 10 |
| 5 | 20以内不退位减法 | 12 | 80% | 个位够减 |
| 6 | 20以内进位加法【凑十法】 | 15 | 80% | 看大数、分小数、凑成 10、加剩数 |
| 7 | 20以内退位减法【破十法】 | 15 | 80% | 个位不够减时，把十几分成 10 和几 |
| 8 | 20以内进退位混合 | 18 | 75% | 凑十 + 破十 |

家长可在顶部「换关卡」跳转。本关正确率不够会重刷，不硬闯下一关。

---

## 3. 出题怎么排

### 闯关：打散，求舒服

以前先随机抽一个加数，再抽另一个，`9+1`、`8+1` 会偏多。现在改为：

- 从本关**全部合法题目**里均匀抽
- 本关做过的题先不重复
- 下一题换开头的数，避免 `9+1` 接着 `9+2`
- 避免紧挨着出交换题（`3+5` 接着 `5+3`）
- 避免两道题数字都只差 1

8 个关卡共用这套规则。

### 针对性练习：有故事顺序

小熊安排的大约 10 道，不打乱，按学习节奏：

1. **先热热身**  
   用更简单、能铺垫的题。例如要练 `8+6`，先 `8+2` 凑成 10；年级够了再 `10+4`。要练 `13-5`，先 `10-5`。热身不超当前年级（第一关不会突然出 20 以内）。
2. **练练这道**  
   真正容易错的那一题。凑十 / 破十会先听思路。
3. **同类练熟**  
   同一类换数字，仍避开无意义连号。
4. **加法和减法分成两段**  
   先把凑十块做完，再做破十，不来回切。

页面提示会写成：先热热身 / 练练这道 / 同类练熟。

---

## 4. 讲思路：十格图 + 凑十 / 破十

加减对错用 `a+b` / `a-b` 当场算。讲解数字必须和得数一致。

**凑十（如 8+6）**

- 看大数 8，还差 2 到 10
- 把 6 分成 2 和 4
- `8+2=10`，`10+4=14`

**破十（如 13-5）**

- 个位 3 不够减 5
- 把 13 分成 10 和 3
- `10-5=5`，`5+3=8`

个位是 0 的整十减（如 `20-5`、`10-3`）不走「分成 10 和 0」，直接从整十里减，避免讲错。

十格图上的点数与算式一致。孩子可点「听思路」「再听一遍」。语音用浏览器自带朗读（Web Speech），不装插件。iPad 侧边静音键仍可能让朗读没声音。

---

## 5. 小熊老师（AI）做什么、不做什么

模型：`deepseek-v4-flash`，关掉深度思考。只在家长点「小熊分析」时调用一次。

**做的事**

- 看今日题数、对错、有没有听思路、卡在哪一关、错题本
- 给孩子一句短鼓励
- 给家长两三句：卡在凑十、破十，还是某几道口算
- 列出最多 5 道薄弱题（如 `8+6`）
- 指定明天练哪 1～2 类关卡题型

**不做的事**

- 不算加减、不改对错
- 不讲每一题、不代替十格图动画
- 不陪聊、不扮演孩子的另一个身份
- 不发明新技能、不出超纲题

连不上模型时，按错题本在本地排题，练习仍能继续。

密钥只放服务器 / 本机 `.env`，不写进网页、不提交 GitHub。页面只请求自家 `/api/analyze`。

---

## 6. 练习数据存在哪

全部在**这台设备的浏览器** `localStorage`，键名 `math20data`。没有账号，也不在服务器建库。

会保存：星星、关卡、本关进度、今日统计、错题本、今日每题记录（最多 80 条）、小熊上次的安排、语音开关。

换日会清今日题数和当日明细；星星、关卡、错题本、小熊安排还在。家长「一键清空」才全部抹掉。

换设备或清浏览器数据等于重新开始。点「小熊分析」时，当日记录只临时发给本机接口，分析完不落库。

---

## 7. 界面与反馈

- 大键盘、大算式，方便 iPad；横屏时题目在左、键盘在右。
- 点小熊会加油；通关有抱抱动画（`hug.gif`）。
- 答对 / 答错弹出 **GIF 表情贴纸**（开心一组、哭脸一组，随机且尽量不重复）。贴纸在答案框右侧，不挡住算式数字。
- 错题本可再练，做对就移走。

---

## 8. 技术结构

```
浏览器（index.html + app.css + hug.gif + emoji/*.gif）
    │
    ├─ 闯关 / 判分 / 动画 / 存 localStorage     ← 全部本地
    └─ POST /api/analyze                      ← 仅分析
            │
            python server.py
            └─ DeepSeek（密钥在 .env）
```

本机：`python server.py`，端口看 `.env` 里的 `PORT`，浏览器打开对应地址。

服务器若 Nginx 继续用 8088 提供静态页，把 `/api/` 反代到本机 Python。网页不要直连大模型。

---

## 9. 故意不做的

- 每日 30 题上限（会挡住想多练的孩子）
- 用大模型生成每一道题或讲解长文
- 把孩子做成「另一个 AI 身份」
- 单独再编一套 SKILLS 编排平台（关卡顺序已经是能力编排）

以后若要更细，优先在现有关卡上加「某道口算的掌握度」（例如 `8+6` 错了 3 次就优先出现），而不是另起一套系统。
